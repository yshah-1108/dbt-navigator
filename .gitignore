__pycache__/
.pytest_cache/
*.pyc
.DS_Store

# The private leak-term list. Never commit: it is an inventory of internal
# identifiers, which is exactly what the scan using it exists to keep out of
# this repository.
.leakcheck-terms
